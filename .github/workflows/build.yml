name: Build

on:
  push:
    branches:
      - master
      - docker-build

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      # - name: Cache Docker layers
      #   uses: actions/cache@v2
      #   with:
      #     path: /tmp/.docker-buildx-cache
      #     key: ${{ runner.os }}-docker-buildx-${{ github.sha }}
      #     restore-keys: ${{ runner.os }}-docker-buildx-

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: docker.pkg.github.com/${{ github.repository	}}/application:sha_${{ github.sha }}
          # cache-from: type=local,src=/tmp/.docker-buildx-cache
          # cache-to: type=local,dest=/tmp/.docker-buildx-cache
